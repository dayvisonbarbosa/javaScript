

function vetores() {
    let vet = [1, 2, 3]   

     /* vet.push(1) - .push é o método automático que cria índices e coloca o elemento dentro dele. Nesse caso o elemento é o número 1. */
    
     /* vet.push(100)
     window.alert(`O vetor ${vet} está ocupando a posição ${vet.length}`) - .length é o atributo que diz quantas posições tem um vetor, ou em que posição determinado elemento está em um vetor. */

    /* window.alert(`O vetor tem ${vet.length} posições.`) */

    /*window.alert(`O primeiro valor do vetor é ${vet[0]}`) - Mostra o valor armazenado em um vetor. */
}